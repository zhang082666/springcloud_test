server:
  port: 8082

spring:
  application:
    name: order-service #服务名
#连接注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7070/eureka/
  instance:
    prefer-ip-address: true  #支持域名直接解析ip